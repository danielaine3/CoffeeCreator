<h1>Coffee Creator!</h1>
<h2> Create a Coffee then Drink It!</h2>

<h3>Add a Coffee</h3>
<form class= "create-form">
	<div class= "form-group">
		<label for ="co">Coffee Drink:</label>
		<input type ="text" id ="co" name="name">
	</div>
	<button type="submit" class="add-coffee">Add coffee!</button>
</form>

<h3>Coffee ready to drink!</h3>

<ul>
	{{#each coffee}}
		{{#unless drank}}
			{{> coffee/coffee-block drank=true}}
		{{/unless}}
	{{/each}}
</ul>

<h3>Coffee that's already gone.</h3>

<ul>
	{{#each coffee}}
		{{#if drank}}
			{{> coffee/coffee-block drank=false}}
		{{/if}}
	{{/each}} 
</ul>

